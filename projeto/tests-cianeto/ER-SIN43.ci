/* 
    Lucca Carvalho
    luccalafonte97@gmail.com
*/

@ce(6, "não existe a declaração de packages",
   "function or field declaration expected", "comp.Compiler.classDec()")

class Program 

    package m;

    func run {
    }

end