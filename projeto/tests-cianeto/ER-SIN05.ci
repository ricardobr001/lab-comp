/* 
Isabela Salmeron Boschi
isabelasalmeron@msn.com
*/
@cep(8, "express�o ausente depois da ','", "Expression expected", "comp.Compiler.factor()")
      
class Program

    func run() {
      
        var Int i, j;
        read(i, j, );
       
    }

end
