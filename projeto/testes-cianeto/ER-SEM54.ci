@cep(10, "An overridden method should be preceded by 'override'")

open 
class A
    public func zero -> Int { 
        return 0;
    }
end

class B extends A
    public func zero -> Int {
        return 1; 
    }
end

class Program 
    func run {
    }
end
